import React from 'react';
import styles from './weaponView.module.css';

const WeaponView = (props) => {
  return (
    // <form className={styles.attributes} onChange={props.onChange || null}>
    //   <table>
    //     <thead>
    //       <tr>
    //         {
    //         atts.map(att => {
    //           <td>{att.label}</td>
    //         })}
    //       </tr>
    //     </thead>
    //     <tbody>

    //         <tr>
    //           <input type='text' id={att.id} defaultValue={props[att.id] || ''} />
    //         </tr>

    //     </tbody>

    //   </table>
    // </form>
    <p>Weapon</p>
  );
};

export default WeaponView;
